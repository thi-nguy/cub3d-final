#include "cub3d.h"


void    check_argument(int ac, char** av)
{
    if(ac == 3)
    {

    }
    else if (ac == 2)
    {

    }
    else
    {
        ft_putstr_fd("The number of arguments is not correct\n", 1);
        exit(ERROR);
    }
}